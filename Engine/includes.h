#pragma once
#include <stdlib.h>
#include <string.h>
#include <math.h>
// #define NDEBUG
#include <assert.h>

#define TRUE 1
#define FALSE 0
#define BOOL char

#define swap(T, x, y) \
    {                 \
        T tmp = x;    \
        x = y;        \
        y = tmp;}

